[tox]
minversion = 3.24.0
envlist = py310, flake8, mypy, pylint
isolated_build = true

[gh-actions]
python =
    3.10: py310, mypy, flake8, pylint

[testenv]
setenv =
    PYTHONPATH = {toxinidir}
deps =
    -r{toxinidir}/requirements.txt
    -r{toxinidir}/requirements_dev.txt
commands =
    pytest --basetemp={envtmpdir}

[testenv:flake8]
basepython = python3.10
deps =
    -r{toxinidir}/requirements_dev.txt
commands = flake8 src/ tests/

[testenv:mypy]
basepython = python3.10
deps =
    -r{toxinidir}/requirements_dev.txt
commands = mypy src/

[testenv:pylint]
basepython = python3.10
deps =
    -r{toxinidir}/requirements_dev.txt
commands = pylint -j 0 src/ tests/
