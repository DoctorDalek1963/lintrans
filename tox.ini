[tox]
minversion = 3.24.0
envlist = py310
isolated_build = true

[gh-actions]
python =
    3.10: py310

[testenv]
setenv =
    PYTHONPATH = {toxinidir}
deps =
    -r{toxinidir}/requirements.txt
    pytest
    toml
commands =
    pip install -e .
    pytest --basetemp={envtmpdir}
